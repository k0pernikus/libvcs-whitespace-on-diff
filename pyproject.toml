[project]
name = "libvcs-whitespace-on-diff"
version = "0.0.0"
description = "Reproduction: libvcs Git.run() strips whitespace from captured git output"
requires-python = ">=3.10"
dependencies = [
  "libvcs==0.42.0",
]

[dependency-groups]
dev = [
  "pytest>=8",
]

[tool.uv]
package = false
